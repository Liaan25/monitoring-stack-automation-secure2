# 📝 Руководство по переименованию файлов проекта

## Обзор изменений

Все файлы проекта переименованы согласно профессиональным стандартам именования в enterprise-проектах:
- Использование дефисов `-` вместо подчеркиваний `_` для исполняемых скриптов
- Стандартные расширения (`.example`, `.template`) для конфигурационных файлов
- UPPERCASE для документации (`SECURITY.md`, `README.md`)
- Описательные имена, отражающие суть файлов

---

## 📊 Таблица переименований

### Корневая директория

| Старое название | Новое название | Обоснование |
|----------------|----------------|-------------|
| `deploy_monitoring_script.sh` | `install-monitoring-stack.sh` | ✅ Более описательное имя<br>✅ Дефисы вместо подчеркиваний<br>✅ Отражает суть: установка стека мониторинга |
| `SECURITY_IB_NOTES.md` | `SECURITY.md` | ✅ Стандартное имя для GitHub/GitLab<br>✅ Краткое и понятное<br>✅ Используется в enterprise-проектах |
| `sudoers` | `sudoers.example` | ✅ Расширение `.example` указывает на образец<br>✅ Предотвращает случайное использование |
| `sudoers.txt` | `sudoers.template` | ✅ Расширение `.template` для шаблонов<br>✅ Семантически правильное название |

### Директория wrappers/

| Старое название | Новое название | Обоснование |
|----------------|----------------|-------------|
| `config_writer.sh` | `config-writer.sh` | ✅ Дефисы для скриптов (стандарт POSIX)<br>✅ Единообразие с другими файлами |
| `generate_launchers.sh` | `build-integrity-checkers.sh` | ✅ Описательное имя<br>✅ Отражает суть: построение проверяльщиков целостности<br>✅ "build" - стандартный термин |
| `grafana_wrapper.sh` | `grafana-api-wrapper.sh` | ✅ Указывает на работу с API<br>✅ Дефисы вместо подчеркиваний<br>✅ Единообразие с `rlm-api-wrapper.sh` |
| `iptables_wrapper.sh` | `firewall-manager.sh` | ✅ Более профессиональное название<br>✅ "manager" - стандартный термин для управления<br>✅ Не привязано к конкретной реализации (iptables) |
| `rlm_task_wrapper.sh` | `rlm-api-wrapper.sh` | ✅ Указывает на работу с API<br>✅ Дефисы вместо подчеркиваний<br>✅ Единообразие с `grafana-api-wrapper.sh` |

---

## 🎯 Стандарты именования

### 1. Дефисы vs Подчеркивания

**Для скриптов:** используем дефисы `-`
```
✅ install-monitoring-stack.sh
✅ config-writer.sh
✅ firewall-manager.sh
❌ deploy_monitoring_script.sh
❌ config_writer.sh
```

**Для переменных/функций в коде:** используем подчеркивания `_`
```bash
✅ MY_VARIABLE="value"
✅ function_name() { ... }
❌ my-variable="value"
```

**Обоснование:**
- Дефисы в именах файлов - стандарт GNU/Linux и POSIX
- Дефисы легче читать: `build-integrity-checkers` vs `build_integrity_checkers`
- Используется в большинстве open-source проектов (Docker, Kubernetes, etc.)

### 2. Расширения для конфигов

**`.example`** - для примеров конфигурации
```
✅ sudoers.example
✅ config.json.example
✅ .env.example
```

**`.template`** - для шаблонов с переменными
```
✅ sudoers.template
✅ nginx.conf.template
✅ deployment.yaml.template
```

**Обоснование:**
- Стандарт в GitHub/GitLab репозиториях
- Предотвращает случайное использование примеров в продакшене
- Семантически разделяет примеры и шаблоны

### 3. Документация (UPPERCASE)

**Стандартные имена для документации:**
```
✅ README.md         - основная документация
✅ SECURITY.md       - политика безопасности
✅ CHANGELOG.md      - история изменений
✅ CONTRIBUTING.md   - правила контрибьюции
✅ LICENSE.md        - лицензия
```

**Обоснование:**
- Стандарт GitHub (автоматически отображается)
- UPPERCASE выделяет важные документы
- Узнаваемо для всех разработчиков

### 4. Описательные имена

**Плохо:** `script.sh`, `wrapper.sh`, `deploy.sh`
```
❌ Слишком общее
❌ Не понятно что делает
❌ Конфликтует с другими скриптами
```

**Хорошо:** `install-monitoring-stack.sh`, `firewall-manager.sh`
```
✅ Описательное имя
✅ Понятно без документации
✅ Уникальное
```

---

## 🔄 Обновление кода

### Автоматические обновления

Все ссылки в следующих файлах **уже обновлены**:
- ✅ `README.md` - все упоминания старых имен файлов
- ✅ Структура проекта в документации
- ✅ Примеры использования

### Ручные обновления (если используете проект)

#### 1. Jenkins Pipeline (Jenkinsfile)

**Найти и заменить:**
```groovy
// Старое:
deploy_monitoring_script.sh

// Новое:
install-monitoring-stack.sh
```

**Обновить в секциях:**
- `prep_clone.sh` - строка с `generate_launchers.sh` → `build-integrity-checkers.sh`
- `scp_script.sh` - путь к основному скрипту
- `verify_script.sh` - проверка наличия файла
- `deploy_script.sh` - запуск скрипта

#### 2. Основной скрипт (install-monitoring-stack.sh)

**Найти и заменить внутри скрипта:**
```bash
# Старое:
WRAPPERS_DIR/config_writer.sh
WRAPPERS_DIR/iptables_wrapper.sh
WRAPPERS_DIR/grafana_wrapper.sh
WRAPPERS_DIR/rlm_task_wrapper.sh
WRAPPERS_DIR/generate_launchers.sh

# Новое:
WRAPPERS_DIR/config-writer.sh
WRAPPERS_DIR/firewall-manager.sh
WRAPPERS_DIR/grafana-api-wrapper.sh
WRAPPERS_DIR/rlm-api-wrapper.sh
WRAPPERS_DIR/build-integrity-checkers.sh
```

#### 3. Лаунчеры (генерируются автоматически)

**Имена лаунчеров изменятся на:**
```bash
# Старые:
config_writer_launcher.sh
iptables_launcher.sh
grafana_launcher.sh
rlm_launcher.sh

# Новые:
config-writer_launcher.sh
firewall-manager_launcher.sh
grafana-api-wrapper_launcher.sh
rlm-api-wrapper_launcher.sh
```

#### 4. Sudoers конфигурация

**Старое:**
```
ALL=(ALL:ALL) NOEXEC: NOPASSWD: /bin/bash /tmp/deploy-monitoring/deploy_monitoring_script.sh
```

**Новое:**
```
ALL=(ALL:ALL) NOEXEC: NOPASSWD: /bin/bash /tmp/deploy-monitoring/install-monitoring-stack.sh
```

---

## 📋 Чеклист миграции

При обновлении существующих установок:

### Обязательные шаги:

- [ ] Обновить `Jenkinsfile` (все упоминания старых имен)
- [ ] Обновить `install-monitoring-stack.sh` (пути к оберткам)
- [ ] Обновить `sudoers` на целевых серверах
- [ ] Пересобрать лаунчеры: запустить `build-integrity-checkers.sh`
- [ ] Обновить документацию проекта (если кастомизирована)

### Опциональные шаги:

- [ ] Обновить CI/CD переменные (если используются)
- [ ] Обновить мониторинг (если логирует имена файлов)
- [ ] Уведомить команду о переименовании
- [ ] Обновить внутреннюю документацию/wiki

---

## 🔍 Поиск старых ссылок

### Команды для проверки:

```bash
# Поиск упоминаний старых имен в проекте
grep -r "deploy_monitoring_script" .
grep -r "SECURITY_IB_NOTES" .
grep -r "config_writer\.sh" .
grep -r "iptables_wrapper\.sh" .
grep -r "grafana_wrapper\.sh" .
grep -r "rlm_task_wrapper\.sh" .
grep -r "generate_launchers\.sh" .

# Поиск в Jenkins
# Jenkins → Manage Jenkins → Script Console
# Запустить groovy скрипт для поиска в job конфигах
```

### Где могут остаться старые ссылки:

1. ❗ **Jenkins jobs** - конфигурация pipeline
2. ❗ **Sudoers** на целевых серверах - пути к скриптам
3. ⚠️  **Мониторинг** - логи, алерты, дашборды
4. ⚠️  **Документация** - внутренние wiki, Confluence
5. ⚠️  **Git hooks** - pre-commit, pre-push скрипты
6. ⚠️  **CI/CD переменные** - имена артефактов

---

## 💡 Преимущества новых имен

### Для разработчиков:
- ✅ Легче читать и понимать (`firewall-manager` vs `iptables_wrapper`)
- ✅ Соответствует стандартам GNU/Linux
- ✅ Автокомплит в IDE работает лучше
- ✅ Меньше опечаток (дефис проще подчеркивания)

### Для операторов:
- ✅ Понятно что делает файл без документации
- ✅ Стандартные расширения (`.example`, `.template`)
- ✅ Легко искать в логах

### Для службы безопасности:
- ✅ `SECURITY.md` - стандартное имя для аудита
- ✅ Описательные имена упрощают анализ
- ✅ Четкое разделение шаблонов и примеров

---

## 📚 Дополнительные ресурсы

### Стандарты именования:

- **GNU Coding Standards:** https://www.gnu.org/prep/standards/
- **Google Shell Style Guide:** https://google.github.io/styleguide/shellguide.html
- **GitHub Community Standards:** https://docs.github.com/en/communities

### Примеры в популярных проектах:

- **Docker:** `docker-compose.yml.example`
- **Kubernetes:** `kustomization.yaml.template`
- **Nginx:** `nginx.conf.example`
- **Apache:** `httpd.conf.template`

---

## ❓ FAQ

**Q: Нужно ли обновлять старые установки?**  
A: Нет, старые установки продолжат работать. Новые имена нужны только для новых развертываний.

**Q: Как обновить Jenkins pipeline?**  
A: Просто замените старые имена файлов на новые в `Jenkinsfile` и сделайте commit.

**Q: Что делать с уже работающими серверами?**  
A: Можно оставить как есть или обновить sudoers и переразвернуть с новым кодом.

**Q: Изменились ли хеши SHA256?**  
A: Нет, если содержимое файлов не изменилось. Только имена файлов изменились.

**Q: Обратная совместимость?**  
A: Нет, старые имена больше не работают. Но миграция простая - просто замена имен.

---

## ✅ Итоговая структура

```
new_deploy_sh_1/
├── README.md                          ← Обновлен
├── SECURITY.md                        ← Переименован из SECURITY_IB_NOTES.md
├── RENAME_GUIDE.md                    ← Этот файл (новый)
├── Jenkinsfile                        ← Требует обновления
├── install-monitoring-stack.sh        ← Переименован из deploy_monitoring_script.sh
├── sudoers.example                    ← Переименован из sudoers
├── sudoers.template                   ← Переименован из sudoers.txt
└── wrappers/
    ├── build-integrity-checkers.sh    ← Переименован из generate_launchers.sh
    ├── config-writer.sh               ← Переименован из config_writer.sh
    ├── firewall-manager.sh            ← Переименован из iptables_wrapper.sh
    ├── grafana-api-wrapper.sh         ← Переименован из grafana_wrapper.sh
    └── rlm-api-wrapper.sh             ← Переименован из rlm_task_wrapper.sh
```

---

<div align="center">

**Профессиональное именование = Профессиональный проект**

*Создано для упрощения работы команды*

</div>
